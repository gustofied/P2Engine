module Lab1point5 where

import Daml.Script
import DA.List

function1: Int -> Int -> Int
function1 x y = x + y


testFunction: Script()
testFunction = script do
    let
        listString = ["Adam", "Sioud"]
    debug $ " Mitt navn :" <> show (last listString)
    debug $ function1 1 2
    debug $ (\n m -> n + m) 2 3